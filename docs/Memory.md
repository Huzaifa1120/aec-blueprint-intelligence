# Memory — Project Progress Tracker

> Created once coding begins. The AI updates this at the end of every working session so a fresh chat/agent can resume without re-reading the codebase.
>
> **Before starting work:** read this file first. If it's stale, skim `docs/` and recent commits.

---

## Project snapshot

- **Project:** AEC Blueprint Intelligence System (construction QTO / cost estimating)
- **Docs of record:** `docs/PRD.md`, `docs/Architecture.md`, `docs/Rules.md`, `docs/Phases.md`, `docs/Design.md`, `docs/AEC-Blueprint-System-Design-Spec-v3.md` (**v3 supersedes v2 and the original spec**)
- **Current phase:** Phase 4 — Plumbing & Fire Protection (🟦 executed 2026-08-24 on `feature/phase-4-plumbing-fire-protection`, final review APPROVED after fix wave; **awaiting merge decision + human-confirm queue**). Suite: **300 passed + 1 xfail**, ruff clean, HEAD `9aac532`.
- **Status summary:** Phase 2 complete. Phase 2.5 closed 2026-08-23. Phase 3 implemented on `feature/phase-3-mechanical-hvac`. Phase 3.5 closed 2026-08-23 on `feature/v3-conformance-gap-closure`: persistence spine + replay gate, LAYER/SCHEDULE_BLOCK/TEXT_ANNOTATION schema, layer registry, legend/schedule parser, text–layer walker, UNMAPPED tiering, JSON/XLSX/PDF exports, narrated scope of work — all wired into the real app and integration-tested (223 passed + 1 xfail).
- **Python:** machine-dependent. This machine (user `saada`): venv EXISTS at `backend/.venv` (Python 3.13.1, full deps incl. opencv-python-headless; scikit-learn REMOVED 2026-08-22). Invocation convention: `backend/.venv/Scripts/python.exe -m ...` run from `backend/` — system python lacks project deps. The `C:\Users\Huzaifa\...` path recorded earlier is stale (different machine).

---

## Progress log

| Date       | Phase | What was done                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Verified by                                                                                                                                                                |
| ---------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-25 | FE    | **Browser e2e landed via Bun + Playwright (`feature/bun-playwright-e2e`, 5 specs mocked-green).** Spec `docs/superpowers/specs/2026-08-25-bun-playwright-e2e-design.md` + plan `docs/superpowers/plans/2026-08-25-bun-playwright-e2e.md`; phased design owner-approved (Playwright under Bun; two projects one config). Landed: `frontend/playwright.config.ts` (projects `mocked` default / `live` opt-in via `test:e2e:live`, webServer=next dev :3000, Chromium-only); `e2e/fixtures.ts` auto-fixtures (`mockApi` installs `page.route("**/api/**")` unless project=live; `consoleErrors` guard fails specs on console/page errors, favicon-filtered); `e2e/mocks/api.ts` typed handlers mirroring real contracts (`DrawingQualityCheck`/`EstimateListResponse`/`EstimateBoq`/`CatalogEntry`/narration/review-session) — unmocked API calls THROW; vitest.config exclude guards `e2e/**` from unit collection. Coverage: home smoke+nav, estimates index→workspace link, workspace BOQ rows/badges/tabs/export select, catalog browse+search, full upload flow (file→quality badge "45 layers · 3,417 paths"→Run takeoff→redirect to `/estimates/est-e2e-1`). Live project = same specs vs real stack, blocked until owner fixes parked `persist` query-param bug. Verified: eslint 0 errors · tsc clean · vitest 61/61 (no e2e bleed) · prettier clean · next build green · e2e 5/5 incl. headed mode.
| 2026-08-25 | FE    | **Package manager migrated npm → Bun 1.4 (frontend).** Local Bun upgraded 1.1.29→1.4.0 (`bun upgrade`); `package-lock.json` + `node_modules` removed, clean `bun install` → `bun.lock` is now the lockfile of record. `bun update` semver refresh (+`aria-query`); exact pins `next`/`eslint-config-next` bumped 16.3.1→16.3.2 via `bun add`. Majors intentionally NOT taken (flagged to owner): eslint 9→10, typescript 5→7, @types/node ^20→26. Blocked postinstall trusted: `unrs-resolver` (eslint import-resolution native binary). Tooling refs synced: `.githooks/pre-commit` (npm→`bun run`, npx→`bunx`), `frontend/.prettierignore`, `AGENTS.md` commands, `docs/Traps.md`, this file's dev-commands block. | eslint 0 errors (1 pre-existing warning) · tsc --noEmit clean · vitest 61/61 · `next build` green (/ static, /catalog, /design-system, /estimates static + /estimates/[id] dynamic) |
| 2026-08-25 | FE | **Full design-system swap to user-pinned `docs/DESIGN.md` ("Safety Authority" avant-garde world; owner rewrote the doc themselves — do NOT edit it, follow as-is).** Brand = **Huzaifa AEC** (owner ruling; recorded in PRODUCT.md Brand Commitments alongside the pinned visual system). Landed: tokens.css re-pointed to doc palette (Ink Black #12130F / Safety Amber #F5A623 / Guard Green #1F7A53 / Paper #FAF9F5 / Steel #5B6660 / Hazard Red #C43B2E + extended surfaces; legacy class names ink-*/canvas/accent-* kept as aliases so ~25 consumer files reskinned without churn); fonts Archivo Black + Inter + IBM Plex Mono via next/font (Geist removed); radius base 4px (cards 2xl≈7.2px, chips pill 20px); shadows flat-by-default, heavy `0 12px 24px rgba(0,0,0,.4)` reserved for modals (dialog) + large visuals; confidence tiers remapped guard-green/amber/hazard/outline (●◑○— symbols + mono [R] preserved — hard product rule intact); new signature components `GoggleLineDivider` (2px amber wave), `HazardStripe` (diagonal amber/ink tape per owner's reference-site screenshot — bookends header/hero/footer, 15%-opacity overlay on ReexportRequest caution block), `ProtocolCard` (ink/mono/white-10% rows); button variants per doc §8 (amber Primary, 1.5px-ink Outline, solid Hazard Danger); badge chips green-tint/guard pill; Card = service-card pattern (steel border, Archivo title + mini goggle accent, hover shadow-md); AppShell ink header/footer + hazard tape + amber active nav (usePathname, null-guarded for tests) + "Huzaifa AEC" brand; PageHeader Archivo headline-lg + label-mono eyebrow + goggle divider; upload page ink hero block; /design-system showcase rebuilt to document the new world. Finish-review fix wave: amber-on-paper link contrast (estimates list, UnpricedGap, button/badge link variant → ink text + 2px amber underline), workspace metadata title → Huzaifa AEC, ProtocolCard radius 2xl, dialog shadow-lg. Old `docs/AEC-Frontend-Design-System-Spec.md` ("Technical Daylight") is superseded as visual authority by docs/DESIGN.md. | vitest 61/61 · tsc clean · eslint 0 errors (1 pre-existing warning) · next build green (all routes) · impeccable detector `[]` on all changed targets · prettier formatted |
| 2026-08-24 | FE    | **Estimates index page + persistent nav + starter-CSV fix landed (`a7adfbd` backend, `7ba3fd5` frontend, `de9085d` CSV fix; docs this row + spec §3.1 amendment).** New `GET /api/estimates` read-only list (project name + cost totals, name-ordered; Estimate has no timestamp column — noted in endpoint docstring); `/estimates` page with mono-tabular totals table → workspace links, named-failure Retry state, empty-state CTA; `AppShell` renders Estimates + Catalog links on every page (home's one-off Catalog link removed); frontend spec §3.1 amended — owner ruling supersedes "no list page in v1". Starter-CSV fix `de9085d`: old template mixed two schemas in one file (backend DictReader takes first row as header → labor block parsed as garbage); split into two importable templates matching router contract. Known-open: upload flow still broken via UI (`persist` must be query param — one-line fix parked on owner approval). | pytest 2/2 new + ruff clean · vitest 60/60 · tsc · eslint · build green (`/estimates` static + `/estimates/[id]`) |
| 2026-08-24 | 4     | **Phase 4 backend progress observed (parallel workstream):** design spec + 8-task TDD plan committed (`docs/superpowers/{specs,plans}/2026-08-24-phase-4-plumbing-fire-protection*`); ~5 tasks landed — plumbing/fire/fire-alarm layer classification, geometry-derived fittings (discipline-matched tees), fixture-unit accumulation + code-table sizing, `fixture_units` cascade tier, keyword-only `fixture_unit_size` provenance pin. Live API smoke (owner-witnessed): e2e run persisted a 114-item estimate on the MMC electrical sheet; review session→action→close→metrics loop verified live; narration/export/catalog endpoints live. Backend gaps confirmed live: BOQ payload carries no per-row ids/bbox (click-to-source dormant); review actions accept only `{item_id, action, confidence_tier}`. | commits `19d4777`→`a30b5c3`; curl-level verification against uvicorn :8001 |
| 2026-08-23 | FE    | **Frontend v1 PAGES ROUND landed on `main` (commits `9d06024`→`b0c75af`; SDD 4-way parallel builders B/C/D/E + fix wave).** Upload+QualityGate page (`/` — dropzone, verdict badge ●◑○, re-export loop w/ spec §5.1 message verbatim), Estimate Workspace (`/estimates/[id]` — dual-canvas pdf.js viewer w/ §4 Y-flip mapping, resizable TwoPanel, TanStack BOQ table per §5.3 columns, ConfidenceBadge tiers + `[R]`, review cycle accept/reject/correct via real backend contract `{item_id, action, confidence_tier}`, ASSUMED bulk-accept exclusion + virtualizer scroll-to-assumed pulse, export Select JSON/XLSX/PDF, narration tab), Catalog (`/catalog` — import w/ per-row error panel, browse). Frozen degradations: no `/status` endpoint→BOQ-readiness poll; no `source.bbox`→dormant SourceHighlight + inline message. Plan: `docs/superpowers/plans/2026-08-23-frontend-pages-round.md`. Backend-gaps parked: status endpoint; source.bbox; review reason/corrected persistence. | vitest 55/55 · lint 0 errors · tsc clean · build green (/ , /catalog, /design-system, /estimates/[id]); final review APPROVED → fix wave F1–F4 ALL ADDRESSED (scoped re-review) |
| 2026-08-24 | 4     | **Phase 4 brainstorming + plan complete (no code yet).** Scope ruled by owner: ALL of Phase 4 (domestic plumbing + fire suppression + fire alarm); fixtures = existing 5 samples only ("use them all"); all 5 Phase 3/3.5 human gates DEFERRED — design around current behavior. Approach A approved: reuse Phase 3 machinery + exactly 2 engine extensions (geometry-derived fittings from route polylines; fixture-unit accumulation with new `fixture_units` cascade tier between schedule and label). Key probe findings: MMC `FIRE ALARM` OCG is EMPTY (0 paths; only "FIRE EXIT" text) → honest-zero case; `M_SAUDI_RAIN DOWNPIPE` = 44 paths ≈ 11 countable downpipe symbols → only real plumbing geometry on disk. Spec: `docs/superpowers/specs/2026-08-24-phase-4-plumbing-fire-protection-design.md`; plan: `docs/superpowers/plans/2026-08-24-phase-4-plumbing-fire-protection.md` (8 TDD tasks, dependency graph, self-reviewed fixes: load_assembly_rule fixture_units passthrough, persist-route parity for elbows/tees via _size_variables lift, replay coherence via BoqItem→Measurement.route walk, storm_downpipe re-scoped to counted kit — riser length unmeasurable in plan view). ~~Not yet executed.~~ → **EXECUTED same day (next row).** | Spec committed `19d4777`; plan committed `8af1b4a`; owner approved all design sections + approach A in-session |
| 2026-08-24 | 4     | **Phase 4 EXECUTED end-to-end via SDD (8 tasks + fix rounds + final review + fix wave) on `feature/phase-4-plumbing-fire-protection` (HEAD `9aac532`; NOT yet merged/pushed).** Landed: layer classification disciplines plumbing/fire_protection/fire_alarm (`FIRE ALARM` out of electrical; `M_SAUDI_RAIN DOWNPIPE`→plumbing; NCS P-/FP-/FA- families); `app/parsing/fittings.py` geometry-derived elbows/tees with sibling-route tees discipline-gated via cached `discipline_of()` (+2 discriminating goldens); `app/parsing/fixture_units.py` FU accumulation + code-table sizing; cascade tier `fixture_units` between schedule and label; 18 assembly YAMLs incl. water_supply.fixture_unit_gauge; load_assembly_rule passthroughs; e2e `resolve_route_context` triple-return + `_size_variables` lift; deterministic plumbing+fire fixture (typed layers P-FIX-WC/P-FIX-LAV); DoD regression suite (DOWNPIPE_PIN=11 owner ruling, FIRE ALARM honest-zero, FU proof diameter_mm=40 @ fu_total=70, live fitting assertions); replay coherence for fixture_units sizes (tamper→409); Route.size_json String(1000)→**Text** + migration `c37396f6713e`. **App-side correctness found & fixed en route:** pymupdf ≥1.28 forward+reverse stroke items inflated route lengths ≈3× → stroke-dedup + SHAPE_EMISSION_FACTOR 3.0→1.0 rebase in test_phase3_regression (sheet_metal_m2 Σ 146.28→48.76 = drawn truth); stale `route_layers()` set → canonical ROUTE_ASSEMBLIES import; degenerate-route skip; join-point duplicate collapse; M-PIPE label-tier provenance flip pinned. Owner rulings: APPROVED_REBASELINE_COUNTS += storm_downpipe:11; raster-spike truth-set scoped to certified types; device rates $38/0.05 plumbing & $45/0.02 FP+FA confirmed. Gate conditions for first real plumbing/fire sheet: YAML-driven round-capability refactor (acceptance: WATER SUPPLY/SANITARY layers), fitting vertex-provenance persistence or spec amendment. Human-confirm queue open: FU values wc3/lav1/sink2, gauge thresholds, ASSUMED defaults. | Full suite **300 passed + 1 xfail**, ruff clean at HEAD `9aac532`; SDD ledger `.superpowers/sdd/2026-08-24-phase-4-plumbing-fire-protection/progress.md`; every task subagent-reviewed except Task 2 (controller-reviewed during provider outage; final review re-covered fresh — SOUND) |
| 2026-08-23 | 3.5   | **Phase 3.5 final whole-branch review → APPROVE WITH FINDINGS → consolidated fix wave → ALL ADDRESSED.** Landed: BDC/EMC content-stream fallback tier in `probe_span_ocgs` (honest `{}` degradation; strict op↔span lockstep — real MMC sample degrades to `{}` due to merged spans 388/384; `/Resources /Properties` name resolution = future add); effective ASSUMED tier persisted into `Route.size_json`/`BoqItem.size_source`; unit-of-measure through derivation_json→payload→XLSX/PDF columns; single ROUTE_ASSEMBLIES/SIZED_ASSEMBLIES definition in extraction.py; strict name-skip parity in `_persist_component_boq`; fail-closed FormulaValidationError guards on response BOQ loops (drop-not-500); anthropic-missing-key honesty log; spec §4.10/D8/§5 wording truthed (clustering bbox fix = scoped+gated, NOT implemented; provenance chain ends at Component/Route rows until path-id migration). | Full suite **238 passed + 1 xfail**, ruff clean at HEAD `e2b2265` |
| 2026-08-23 | FE    | **Frontend design-system foundation landed (branch `feature/v3-conformance-gap-closure`, commits `6b003d5`→`dc86d10`; WIP twin `feature/frontend-design-system`).** Spec of record `docs/AEC-Frontend-Design-System-Spec.md` (owner-amended: shadcn/ui v4 radix-nova ADOPTED, lucide-react supersedes phosphor). Landed: Technical Daylight tokens (`src/styles/tokens.css`, ink/canvas/accent/tier palette + shadcn semantic mapping, no dark mode), Geist fonts mapped, shadcn scaffold (badge/button/tooltip/skeleton/card), ConfidenceBadge (●◑○— three-signal + `[R]` raster modifier), Empty/Error/Loading states, PageHeader, AppShell (56px), vitest+RTL harness, dev-only `/design-system` styleguide, ReviewOverlay sketch retired. Plan: `docs/superpowers/plans/2026-08-23-frontend-design-system-foundation.md`. Deferred minors: root-level TooltipProvider, swatch-hex drift test. | vitest 12/12 · eslint/tsc/prettier/build all green; SDD task review APPROVED (3 minors deferred) |
| 2026-08-23 | 3.5   | **Phase 3.5 (v3 conformance & gap closure) complete — all 8 code-addressable gaps landed on `feature/v3-conformance-gap-closure`.** Persistence spine (`SheetExtraction` → single-writer transaction) + `/api/estimates/{id}/boq` + determinism-replay gate (409 on any mismatch/corrupt derivation); evaluator/loader hardening; `layers`/`schedule_blocks`/`text_annotations` tables + `layer_id` FKs fed by YAML-driven `classify_layers`; generic `detect_blocks` legend/schedule parser; text–layer walker (`associate_text` + per-span OCG probe); UNMAPPED tiering (unmapped-OCG clusters surface in `unmapped_items`, persist as UNMAPPED Components, never priced — no Measurement refs); JSON/XLSX/PDF exports + narrated scope of work wired into `main.py` via shared `app/estimates/payload.py` (JSON export == /boq byte-for-value). Carried review fixes: pyproject `openpyxl>=3.1` + `pydantic-settings>=2.7`; real migration-table assertion (incl. reexport_requests/review/drawing_quality tables); isinstance constraint check in test_v3_schema. TDD integration suite `tests/test_v3_integration.py` runs the REAL app against generated fixtures (persist → replay → export → narration → unmapped persistence). | Full suite **223 passed + 1 xfail**, ruff clean, zero regressions vs 214 baseline; report `.superpowers/sdd/2026-08-23-v3-conformance-gap-closure-plan/task-9-report.md` |
| 2026-08-23 | 3     | **Phase 3 implemented end-to-end on `feature/phase-3-mechanical-hvac` (SDD waves; HEAD `e9ff7a6`+docs).** Formula engine (restricted AST, fail-closed), rules extension (formula/gauge BOM entries, variables+defaults in YAML, legacy multipliers byte-identical), 4 mechanical YAMLs + M-DUCT/M-PIPE/M-EQPT mappings, size cascade (schedule→label→geometry→ASSUMED w/ provenance) + schedule detection, provenance migration (`routes.size_json`, `boq_items.derivation_json/size_source`), e2e mechanical branch (derivation+size_source on every row), generated HVAC fixture, S101 equipment regression (N=277 pipeline-derived, PENDING HUMAN VERIFY — FUTR 276 suspicious). Full suite 125 passed + 1 xfail; ruff clean. **Human rulings owed before merge:** (1) S101 FUTR count eyeball; (2) clustering centroid-grid contract gap (fix reverts approved lighting 26→23 baseline); (3) confirm ASSUMED default sizes in YAML. Fixed latent `test_health_db` cache leak (cleared lru caches now rebuild from real env). Planted string in test_migrations.py:44 flagged. | Full pytest green after every wave; ruff clean; commits pushed to origin feature branch; ledger `.superpowers/sdd/2026-08-23-phase-3-mechanical-hvac-plan/progress.md` |
| 2026-08-23 | 3     | **Phase 3 brainstorming complete → design spec + implementation plan written.** Design approved in-session: full scope (ducts/pipes/equipment/formulas); size-resolution cascade schedule→label→geometry→ASSUMED with provenance on every number; YAML-declared parameterized formulas evaluated by a restricted AST walker (no eval, fail-closed validation); gauge lookup tables; `routes.size_json` + `boq_items.derivation_json`/`size_source` columns; e2e mechanical branch; validation = exact-math formula tests + cascade unit tests + S101 real-sheet equipment proof + generated deterministic HVAC fixture (scaffolding only, production code must not reference it). Ruled out: Python-hardcoded calculators (violates data-driven rules), full expression engines (YAGNI). Spec: `docs/superpowers/specs/2026-08-23-phase-3-mechanical-hvac-design.md`; plan: `docs/superpowers/plans/2026-08-23-phase-3-mechanical-hvac-plan.md` (10 tasks, TDD, dependency graph). Plan self-reviewed: fixed lookup_gauge int-key bug, max_mm declaration gap, geometry-test pt→mm math, YAML-driven defaults, column types. | Human approved all 3 design sections + approach A; spec §1–§10 internally consistent; plan cross-checked against real code (`rules.py`, `routes.py`, `e2e/router.py`, models, migrations) |
| 2026-08-23 | 3     | **Samples inventory audited (all 5 PDFs probed via pymupdf); sample-related docs synced.** `data/samples/` holds more than the one electrical fixture: `ABC-SC03-S101.pdf` (structural site plan, 45 OCGs) carries direct mechanical equipment layers `M-EQPT-NEW`/`M-EQPT-FUTR` + xref `X-ABC-SC03-SP|M-EQUIP`/`M-Clearances` and electrical xrefs (`E-1LNEQP-C`, `E-DEV`) — viable Phase 3 equipment-count warm-up, but no duct/pipe routes; still need a real HVAC sheet for full Phase 3 DoD. `Addendum3.pdf` re-described correctly: p0 letter text page + pp1–6 oversized drawing exhibits (not a pure Word doc). MMC's `M_SAUDI_*` prefix is Saudi architectural family, not NCS mechanical (`M_SAUDI_VENT_identy` is a naming trap). Updated: `data/samples/README.md` (full layer inventory), `AGENTS.md` (stale "sample absent" claims → all present). | pymupdf probe of every page: OGC counts/sizes/ops/text chars recorded in README; AGENTS.md + README edits match probe output |
| 2026-08-23 | 2.5   | **Phase 2.5 complete — all six items landed on `feature/phase-2.5-implementation` (HEAD `979a7c5`).** Input Quality Gate + `/drawings/{id}/quality` & `/request-reexport` endpoints + flattened/raster twin fixtures; DBSCAN → deterministic union-find clustering (sklearn removed from deps, numpy declared) with human-approved count re-baseline {door 2 / tray 1 / lighting 26}; raster spike → **human ruling A**: NCC non-discriminative (door 727 vs truth 2 at truth-ceiling 0.903 = 363× false positives), raster stays quarantined, ORB/SIFT named successor per spec v3 §7.7A, revisit on first real degraded upload — expressed as expected-xfail test, artifact `docs/superpowers/reviews/2026-08-22-raster-spike-report.md`; `source_quality` column + Alembic migration + e2e stamping + degraded multiplier (0.8); review-time instrumentation + `GET /projects/{id}/review-metrics` (≤10 min/sheet provisional target in config); ultralytics quarantined behind `ENABLE_LEGACY_YOLO=1`. Bonus correctness fix: route lengths pt→paper-mm→real-m conversion (was physically impossible magnitudes; tray now 0.752 m). | Full pytest suite green (63 passed + 1 expected xfail — raster spike per ruling A); ruff clean |
| 2026-08-22 | 2.5   | **Prerequisites done + quality gates enabled.** Recreated `backend/.venv` (Python 3.13.1) with full deps; declared missing deps in pyproject (`python-multipart`, `scikit-learn`); recreated `backend/.env` from example; `alembic upgrade head` OK. Baseline: pytest 28 passed / 13 failed — ALL 13 traced to the absent sample PDF (owner-supplied, fail-by-design). Fixed 60 pre-existing ruff violations incl. 2 latent F821 bugs (`LRModel` in prices.py, `np` in yolo_detection.py); ruff clean. Frontend: fixed ReviewOverlay.tsx broken JSX/type errors (template-literal className, stray-brace h1, trailing comma, ref-in-render, any-typing, dead handleAccept, axios→fetch); build+lint+typecheck green. Added prettier (+format scripts), `npm run typecheck`, and `.githooks/pre-commit` gate (ruff / eslint+tsc+prettier) enabled via `git config core.hooksPath .githooks`. AGENTS.md commands/gotchas refreshed to match. | pytest 28 passed/13 fixture-blocked; ruff exit=0; npm lint+build+format:check green |
| 2026-08-22 | 2.5   | **Docs synced to Design Spec v3.** Updated `PRD.md`, `Architecture.md`, `Rules.md`, `Phases.md`, `Design.md`, this file. Propagated: Input Quality Gate (§7.2), raster split rewrite — OpenCV legend matching default + Detectron2 only for region segmentation, YOLOv8 removed as default (AGPL Enterprise-License exposure) (§7.7/§9), DBSCAN → deterministic distance-threshold union-find clustering (§7.4), rotation-aware OCR (§7.7C), `source_quality` flag in canonical model/data model/UI, review-time-per-sheet metric (§7.13/§15), three new API endpoints (§10), Stage 1.5 raster spike re-scoped into Phases.md as **Phase 2.5** with DoD. Added implementation-status notes marking every v3 delta not yet in code. Old spec files (v2/original/addendum) removed; v3 committed via `feature/docs-spec-v3-alignment` → dev → main. | Cross-checked each claim against `backend/app/**`, `backend/pyproject.toml`, `data/assemblies/`, frontend tree (validation findings below)                                  |
| 2026-08-21 | 2     | **Phase 2 fully closed.** Executed `docs/superpowers/plans/2026-08-21-phase-2-ep3-yr2-plan.md` (Tasks 1–4 all `[x]`): wrote `test_ep3_e2e_pipeline_validation_on_sample` (gates E1–E9) and `test_yaml_rule_persistence_to_db` (gates Y1–Y5) in `backend/tests/test_phase2_regression.py` (now 12 tests). Fixed 2 new ruff violations in the appended code (E741 `l`, F811 `Material` redefinition) — no new violations vs baseline. Merged `feature/phase2-ep3-yr2` → dev → main (`--no-ff`), pushed both, deleted feature branch. Note: `tests/test_phase1.5_regression.py` has a pre-existing pytest collection error (unrelated, present on baseline). | Phase 2 suite 12/12 green; ruff no new violations; `main` == `origin/main` == `a8120a5`, `dev` == `origin/dev` == `b6b3b53`; working tree clean                            |
| 2026-08-21 | 2     | Phase 2 fully pushed to GitHub (feature → dev → main). EP3 + YR2 remaining plan/spec written to `docs/superpowers/` and committed; plan/spec/review logs reorganized under `docs/superpowers/{plans,specs,reviews}/`. Convention recorded: all future plans/specs go in the superpowers folder.                                                                                                                                                                                                                                                                                                                                                           | Phase 2 tests 10/10 green; branches synced; working tree clean                                                                                                             |
| 2026-08-19 | 0     | Created `.venv` (Python 3.13.1). Backend scaffold: `pyproject.toml` (fastapi, uvicorn, pymupdf, shapely), `app/main.py` (`/` + `/health`), tests, ruff clean. Frontend scaffold: Next.js 16 (TS, Tailwind v4, App Router) at `frontend/`, `page.tsx` calls backend `/health`.                                                                                                                                                                                                                                                                                                                                                                             | pytest 2 passed, ruff clean, `npm run build` green, e2e smoke: frontend rendered backend status `healthy`                                                                  |
| 2026-08-19 | 0     | Phase 0 complete: SQLite DB layer (SQLAlchemy models), Alembic migration for initial schema, env files for both projects, git consolidation into single monorepo, sample fixture registered, `/health` reports DB status.                                                                                                                                                                                                                                                                                                                                                                                                                                 | pytest 11 passed, ruff clean, `alembic upgrade head` applies (15 tables), app boots (`/health` → `{"status":"healthy","db":"ok"}`), `npm run lint` + `npm run build` green |

## Open items / next actions

- [ ] **Frontend pages round (next):** Upload + Quality Gate page, Estimate Workspace (TwoPanel + BOQ table + pdf.js click-to-source), Catalog page per spec §5 — design system foundation is ready. Backend dependency: `source.bbox` per BOQ item (spec §5.3, blocking for click-to-source only).
- [ ] **Phase 4 execution next:** plan ready at `docs/superpowers/plans/2026-08-24-phase-4-plumbing-fire-protection.md` (8 TDD tasks) — awaiting owner go-ahead; cut `feature/phase-4-plumbing-fire-protection` from latest main.
- [x] ~~**Phase 2.5 — Spec v3 alignment**~~ ✅ complete 2026-08-23 — all six items landed (see `Phases.md` amendment for the raster-spike ruling A closure). ~~declare or drop scikit-learn~~ → removed from deps 2026-08-23.
- [x] ~~**Phase 3.5 — v3 conformance & gap closure**~~ ✅ complete 2026-08-23 (see `Phases.md` Phase 3.5 section).
- [ ] **Human gates before/with Phase 4:** (1) S101 FUTR=276 visual verify; (2) lighting re-baseline ruling (unblocks clustering bbox fix); (3) hanger-kit semantics; (4) ASSUMED default sizes in YAML; (5) real HVAC sheet supply.
- [ ] **Phase 3.5 deferred minors (final-review triage, none block merge):** `path_ids_json` column on components/routes next migration (provenance currently stops at row granularity); gauge-line legacy-scaling router/persistence parity drift (dormant); linear_per_m rows lack rule_version stamp; JSON String-length truncation → Postgres portability; NULL-name sheet collapse + stale empty Estimate shells per re-run; `load_assembly_rule` list-root runtime coercion unreachable guard; LayerRow not frozen dataclass; schedule-detector limits (global tolerance anchor / same-y band merge / no vertical gap cutoff); text_walker coord KeyError inconsistency + broad excepts; BDC/EMC `/Resources /Properties` OC-name resolution + merged-span recall heuristic.
- [ ] ORB/SIFT raster successor — trigger on first real degraded upload (spec v3 §7.7A; human ruling A).
- [ ] `labor_rates` model drift unmigrated (autogenerate strips it each time) — register or exclude LRModel.
- [ ] `renderer.py` reshape bug (4-channel) — `template_match` shim compensates; one-line upstream fix owed.
- [x] Restore sample PDF to `data/samples/` from the project owner (restored 2026-08-22; suite 63 green).
- [ ] Decide: delete redundant remote branch `feature/phase-2.5-spec-v3-alignment` (zero unique commits; superseded by `feature/phase-2.5-implementation`).
- [ ] Optional: apply `ruff format` across backend (not mass-run yet — avoids churn).
- [ ] Confirm price catalog source + single-tenant deployment with owner.
- [ ] Post-Phase-0: PostgreSQL migration + object storage.

## Dev commands (Phase 0 baseline)

```bash
# One-time: activate the venv (from repo root, Git Bash)
source backend/.venv/Scripts/activate
# Prompt changes to (venv) ... ; verify: which python → backend/.venv/Scripts/python

# Backend — venv lives INSIDE backend/ (self-contained)
cd backend
#   Option A (recommended — activate first, then plain commands):
python -m uvicorn app.main:app --reload --port 8000
#   Option B (no activation — full path):
backend/.venv/Scripts/python -m uvicorn app.main:app --reload --port 8000
#   --reload = FastAPI "hot reload": file save → auto-restart

# Tests + lint (from backend/)
python -m pytest -q
python -m ruff check app tests

# DB migrations (from backend/, uses DATABASE_URL from .env)
python -m alembic upgrade head          # apply pending migrations
python -m alembic revision --autogenerate -m "describe change"   # new migration

# Deactivate when done (only if you activated)
deactivate

# Frontend (from frontend/) — Bun is the package manager (migrated from npm 2026-08-25)
bun install          # lockfile = bun.lock; never reintroduce package-lock.json
bun run dev          # http://localhost:3000 — Turbopack, Fast Refresh (save → instant update)
bun run build && bun run start   # production preview
```

The two dev servers run independently; the frontend reaches the backend at `NEXT_PUBLIC_API_URL` (default `http://127.0.0.1:8000`).

## Decisions that must not be re-litigated

- Hybrid, vector-first, rules-driven, human-verified architecture. **Spec v3 framing:** vector-first is the *preferred* path when available, not the *assumed default input* — every upload passes the Input Quality Gate first.
- No LLM/vision model ever outputs a final quantity (see `Rules.md`).
- **Ultralytics YOLOv8 is NOT in the default stack** — AGPL-3.0 requires an Enterprise License even for internal-only proprietary use (vendor-confirmed, spec v3 §7.7/§9). Raster = OpenCV legend matching + Detectron2 (Apache-2.0) + rotation-aware OCR.
- **Clustering target is deterministic distance-threshold union-find**, not DBSCAN (spec v3 §7.4); DBSCAN in `vector.py` is legacy pending Phase 2.5 migration.
- Raster fallback history: Phase 1.5 done with the old technique; v3 re-proofs it via spike before production reliance.
- Closed/internal single-company first; generalization is a later decision (strengthened by spec v3 §5.5: only closed deployment makes the re-export loop-back actionable).
- Import PyMuPDF as `pymupdf`, never `fitz`.
- SQLite file-based DB for Phase 0 (works serverless + server); PostgreSQL swap later via `DATABASE_URL` (owner decision 2026-08-19).

## Known issues / gotchas

- **Code-vs-v3 gaps (updated 2026-08-23 — most closed by Phase 2.5 implementation):** ~~`classify_upload()` binary vector/raster heuristic, no layer-richness scoring~~ → Input Quality Gate landed (flattened-file calibration beyond the one sample still open); ~~`vector.py` sklearn DBSCAN (`eps=5.0, min_pts=2`)~~ → deterministic distance-threshold union-find landed w/ human-approved re-baseline {door 2, tray 1, lighting 26}; ~~no `source_quality` column anywhere in DB models~~ → `source_quality` column + endpoints landed; ~~no review-time logging or `/quality`, `/request-reexport`, `/review-metrics` endpoints~~ → review instrumentation + `/review-metrics` landed; ~~ultralytics importable by default~~ → quarantined behind `ENABLE_LEGACY_YOLO=1`; scikit-learn removed, numpy declared, opencv-python-headless added.
- **Closed by Phase 3.5 (2026-08-23):** ~~`LAYER`/`SCHEDULE_BLOCK` tables designed but not migrated~~ → migrated + persisted (`layers`, `schedule_blocks`, `text_annotations` + `layer_id` FKs); Detectron2 Stage-2 scope remains future work.
- **Environment (fixed 2026-08-22):** `backend/.venv` recreated with all deps; `python-multipart` + `numpy` declared in `pyproject.toml`, scikit-learn removed and `opencv-python-headless` added; `openpyxl>=3.1` + `pydantic-settings>=2.7` declared 2026-08-23 (final dep state 2026-08-23); `backend/.env` recreated from `.env.example`. Sample PDF restored to `data/samples/` 2026-08-22 (still a gitignored client drawing by design). Heavy ML deps (`ultralytics`, `detectron2`, `paddleocr`) are intentionally import-gated optionals — code degrades gracefully without them; ultralytics additionally quarantined behind `ENABLE_LEGACY_YOLO=1`; install ad hoc only when needed.
- Live API routes: `/`, `/health`, `POST /api/e2e/run` (+persist), `POST /api/catalog/import`, `GET /api/catalog/`, drawings quality endpoints, review metrics, `GET /api/estimates/{id}/boq|replay`, `GET /api/exports/estimates/{id}/export?format=json|xlsx|pdf`, `GET /api/narration/estimates/{id}`.
- 8 assembly YAML files exist in `data/assemblies/` (Phases.md previously miscounted 9; fixed).
- **All plans, specs, and code-review logs live in `docs/superpowers/{plans,specs,reviews}/`** — written with the superpowers workflow (brainstorming → spec, writing-plans → plan). Never create plan/spec docs elsewhere. Existing plan/spec docs moved there 2026-08-21 (`2026-08-20-phase-2-electrical-plan/spec.md`, `2026-08-21-phase-2-remaining-plan/spec.md`, `2026-08-21-phase-2-ep3-yr2-plan/spec.md`, etc.). File reorganization also 2026-08-21: `implementation_plan.md` moved to `docs/superpowers/plans/`, root `new.md` renamed to `architecture_decision.md`, `GIT_WORKFLOW.md` renamed to `git-workflow.md`, `AEC-Blueprint-System-Design-Spec.md` renamed to `AEC-System-Design-Spec.md`. Orphan placeholder `phase-0-foundation-feature.txt` removed.
- venv lives at `backend/.venv` when present. Use `python -m <tool>` instead of `<tool>.exe` — console-script exes (pytest.exe, ruff.exe) embed the old absolute path and break after a move.
- `pip install --upgrade pip` inside a running venv failed with WinError 32 (file locked). Recreate venv + install instead; skip pip upgrades.
- pip 24.3 ignores PEP 735 `[dependency-groups]`; dev tools (pytest, ruff) installed explicitly.
- Single git repo at the root; the nested `frontend/.git` from `create-next-app` was removed so backend + frontend + docs share one history.
